require 'spec_helper'

describe SuperAwesomePrint do
  it 'has a version number' do
    expect(SuperAwesomePrint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
